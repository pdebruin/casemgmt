# XRM Change Requests from CaseMgmt

| # | Feature | Status | Notes |
|---|---------|--------|-------|
| — | No changes required | ✅ | Existing `IRecordLifecycleHandler.OnUpdatedAsync` provides entity name, record ID, old/new JSON — sufficient for BPM integration |
